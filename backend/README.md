# OpenSAP Copilot Backend

Clean v0.2.1 backend foundation.

Run from repository root with `START_BACKEND.bat`, then open:

`http://127.0.0.1:8000/docs`

Security: use only synthetic, anonymized, or explicitly permitted ABAP source code.
